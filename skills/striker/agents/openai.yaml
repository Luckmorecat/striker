interface:
  display_name: "Striker"
  short_description: "Run the project-local Striker CLI"
  default_prompt: "Use $striker to operate the project-local Striker CLI."
policy:
  allow_implicit_invocation: false
