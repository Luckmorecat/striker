interface:
  display_name: "Striker Shape"
  short_description: "Shape an idea into an approved product brief"
  default_prompt: "Use $striker-shape to turn this idea into an approved brief."
policy:
  allow_implicit_invocation: false
